# SpeedInspect 项目规则

统一规则入口是仓库根目录的 [AGENTS.md](../../AGENTS.md)。

开始任务先读 `docs/PROJECT_STATUS.md`、`docs/技术方案.md` 和 `docs/ROADMAP.md`。默认在 `dev` 开发，生产目标为 Vercel + 托管服务。当前后端是 FastAPI / SQLAlchemy，不是旧规则中描述的 MongoDB / Node 服务。版本以锁文件为准，不能沿用虚构的测试覆盖率或完成状态。验证、权限、交接和发布规则均以根 `AGENTS.md` 为准。
