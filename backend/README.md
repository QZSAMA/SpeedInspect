# Backend Services
所有后端服务都放在这个目录下，采用FastAPI微服务架构。

## 服务列表
- `user-service/` - 用户服务，负责账号管理、鉴权
- `order-service/` - 订单服务，负责支付、订单管理
- `report-service/` - 报告服务，负责报告生成、存储、分享
- `agent-service/` - Agent调度服务，负责AI Agent任务调度
- `common/` - 公共库，所有服务共享的代码
